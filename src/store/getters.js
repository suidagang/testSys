const getters = {
    sidebar: state => state.one.sidebar,
    language: state => state.one.language,
    twoName: state => state.two.name,
    threeName: state => state.three.name,
}
export default getters